K近邻核心思想是未标记样本的类别由距离其最近的K个邻居投票来决定。
方法：计算为标注的样本和数据集中每个样本的距离，取距离最近的K个样本，然后进行投票得到标签。
算法优缺点：
    优点：准确性高，对异常值和噪声有较高的容忍度；
    缺点：计算量大，对内存需要需求大。

经验：K值的选择至关重要，K值过大，模型的偏差越大，对噪声数据越不敏感，K很大时可能会造成模型欠拟合；
    K值越小，模型的方差就会越大，当K值太小，就会造成模型过拟合；

