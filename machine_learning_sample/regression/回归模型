回归是从输入数据中预测连续值输出。
举例如下：
股价预测
月销售额预测
机票预测
社交媒体预测喜好和评价

一、简单回归
    线性回归：拟合一条直线表示输入和输出的关系。
    房价预测中，假定房屋价格与房屋面积成正比，则y~=f(x)
            yi = f(xi) + ei
    ei是第i个房子的预测误差，误差值取决于样本，最终希望误差的期望值为0 E(ei)=0
    若f(xi) = bi * xi + b0
    则 RSS = sum(1, N, (yi-f(xi))^2) =sum(1, N, (yi- (bi * xi + b0)^2)
    使用随机梯度下降法进行优化


二、多元回归
    指具有多重特征的线性回归，及数据集中包含多个对预测输出同样重要的特征。
    多项式回归如下：
            yi = b0 + b1 * x + b2 * x^2 + ...+ bp * x^p + ei
        参数=[b0, b1, b2,...,bp]
        特征=[x, x^2, x^3,..., x^p]

    最小二乘法求解

三、模型评价
    训练误差：平均训练误差、均方根误差Root Mean Squared Error
    泛化误差：完整数据集上的误差
    测试误差：测试集上的误差

    过拟合Overfitting：训练集上准确度好，测试集上差

    不可约误差：不同人对同一事物的不同评价
    偏差bias：真实函数与数据平均拟合的差值。偏差意味着平均拟合程度；
        低复杂度的模型具备很高的偏差。
    方差variance：数据上平均拟合和针对性拟合之间的变换。

    MSE均方误差=bias^2 + variance


